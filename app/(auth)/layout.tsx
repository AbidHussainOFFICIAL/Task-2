/**
 * Auth layout — minimal wrapper.
 * Each auth page sets its own metadata via export const metadata.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children
}
